@model Application.ViewModels.SaveGeneroViewModel
@{
    ViewData["Title"] = "DeleteGen";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>¿Está seguro que desea eliminar el genero?<img style="padding-left: 10px;" src="~/Image/eliminar.png" width="25" height="25" /></h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="Streaming" asp-action="DeleteGenPost">
                    <input type="hidden" asp-for="Id" />

                    <button type="submit" class="btn btn-danger" style="padding-right: 10px;"><img src="~/Image/eliminar.png" width="13" height="13" />  Eliminar</button>
                    <a asp-controller="Streaming" asp-action="Generos" class="btn btn-warning">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

